mixed-port: 7890
allow-lan: false
mode: rule
log-level: info
external-controller: 127.0.0.1:9090

# Провайдеры прокси
proxy-providers:
  my_providers:
    type: http
    url: "https://raw.githubusercontent.com/micromas/v2ray-config-collector/refs/heads/main/CASCAD/providers.yaml"
    path: "./providers.yaml"
    interval: 86400
    health-check:
      enable: true
      url: "https://www.gstatic.com/generate_204"
      interval: 300
      timeout: 5000
      lazy: true
      expected-status: 204

proxy-groups:
  - name: 🚀 Auto
    type: select
    use:
      - my_providers
    proxies:
      - DIRECT
  - name: All Servers # Новая группа для ручного выбора
    type: select
    use:
      - my_providers # Эта группа использует все прокси из my_providers
    # Если вам нужен конкретный порядок или вы хотите выбрать определенные серверы,
    # можно указать их здесь, но для полного списка этот подход удобнее.

# Правила
rules:
  - DOMAIN-SUFFIX,google.com,🚀 Auto
  - DOMAIN-SUFFIX,github.com,🚀 Auto
  #- MATCH,DIRECT
  # Добавьте правило, которое будет направлять весь остальной трафик в вашу группу для выбора
  - MATCH,All Servers # Весь остальной трафик будет направляться в группу, где вы можете выбрать сервер
