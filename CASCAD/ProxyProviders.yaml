# Указываем proxy-providers
proxy-providers:
  my_providers: # Название вашего провайдера
    type: http
    url: "https://raw.githubusercontent.com/micromas/v2ray-config-collector/refs/heads/main/CASCAD/providers.yaml"
    interval: 86400  # Обновление раз в сутки
    # path: "proxies" # Если ссылка напрямую дает список прокси, этот параметр не нужен
    health-check:
      enable: true
      interval: 300  # Проверка раз в 5 минут
      url: "https://www.gstatic.com/generate_204"
      # В Mihomo можно добавить 'lazy: true' если хотите проверять только когда прокси используется

# Основной proxy-groups
proxy-groups:
  - name: Auto
    type: fallback # Или select, shuffle...
    # Здесь должны быть ТОЧНЫЕ имена прокси из файла providers.yaml
    # Например, если в providers.yaml есть прокси с именами "Proxy-1" и "Proxy-2"
    proxies:
      - 11💰Amsterdam # <-- Замените на реальное имя прокси из providers.yaml
      - 12💰Amsterdam # <-- Замените на реальное имя прокси из providers.yaml
      - 13💰Amsterdam # <-- Замените на реальное имя прокси из providers.yaml
      - 18💰Amsterdam # <-- Замените на реальное имя прокси из providers.yaml
      - 19💰Amsterdam # <-- Замените на реальное имя прокси из providers.yaml
      - 21💰Amsterdam # <-- Замените на реальное имя прокси из providers.yaml
      # Можно добавить и другие группы прокси из этого же провайдера, если они есть
      # - "another_proxy_from_my_providers"
      - DIRECT # Всегда оставляем DIRECT для прямого подключения
